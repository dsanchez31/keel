{
  // The build tooling, which runs on Node. Tailwind 4 has neither
  // tailwind.config.ts nor postcss.config.js: the theme is src/index.css.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"]
  },
  "include": ["vite.config.ts"]
}
